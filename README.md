Loading two sparse matrices from an input file.
Performing addition, subtraction, and multiplication on the matrices.
In order to run code for sparse matrices, one must run the (sparse_matrix.py) in the folder the file (sparse_matrix.py) is located in.
